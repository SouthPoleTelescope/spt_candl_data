; ACT DR6 lensing has no nuisance parameters of its own and the cosmology
; is fixed in act-dr6-lens-values.ini, so this priors file is intentionally
; empty. The alpha(k) smoothness prior is applied internally by the alphak
; module via its smoothness_sigma option.
